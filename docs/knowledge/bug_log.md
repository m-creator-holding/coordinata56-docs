# Bug Log — coordinata56

Журнал дефектов, зафиксированных QA. Каждый дефект содержит: описание, шаги воспроизведения, ожидаемый/фактический результат, приоритет, статус.

---

## Статус: нет открытых P0/P1 на Батч A

По результатам шага 5 (расширение тестов до мини-DoD) все 212 тестов зелёные.
Потенциальные дефекты, проверенные и закрытые как «не баг»:

### Проверка 1 — PUT /house-types/{id}/options: аудит

**Вывод**: аудит-запись создаётся с `entity_type="HouseTypeOptionCompat"`, не `"HouseType"`.
Это корректное поведение — операция на junction-таблице.
Тест скорректирован под правильный `entity_type`.

### Проверка 2 — DELETE /houses/{id}: audit before

**Вывод**: `house.service.delete()` пишет `changes={"before": before}` в audit.
Тест `test_house_delete_audit_contains_before` прошёл зелёным.

### Проверка 3 — StageRepository.has_references: silent except

**Вывод**: `except Exception: pass` на проверке BudgetPlan (таблица может не существовать
в тест-БД) — не баг, а намеренная защита. Зафиксировано в tech-debt (P3-5).

---

_Следующий баг-лог будет вестись по мере обнаружения дефектов в Батче B/C._
