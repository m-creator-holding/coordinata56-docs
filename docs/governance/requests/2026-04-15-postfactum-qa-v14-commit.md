# Заявка post-factum: qa-работа в коммите регламента v1.4 без ревью

**Дата:** 2026-04-15
**Инициатор:** Координатор (post-factum)
**Тип:** post-factum (быстрый путь, §«Быстрый путь» `departments/governance.md`)
**Статус:** approved (ретроактивно)

## Инцидент

2026-04-15 в ходе работы над регламентом v1.4 фоновый субагент `qa` Батча A застейджил свои файлы (тесты / отчёт), и последующий `git add -A` Координатора подобрал эти файлы в коммит регламента v1.4. Файлы qa не прошли ревью.

## Источник фиксации

`/root/coordinata56/CLAUDE.md`, секция «Git»:
> «Когда фоновые субагенты работают — НЕ делать `git add -A`. Они могут застейджить свои файлы, и ваш коммит подберёт чужую работу до её ревью. Стейджить только конкретный список файлов. (Поймано: 2026-04-15, qa Батча A попал в коммит регламента v1.4 без ревью.)»

## Что меняется (post-factum)

Правило уже добавлено в `CLAUDE.md` непосредственно после инцидента — «быстрый путь» использован корректно. Настоящая заявка — процессное оформление post-factum, как требует §«Быстрый путь»: в течение 24 часов после срочной правки CLAUDE.md подать заявку, которую аудитор ревьюит.

## Почему

Регламент `departments/governance.md` требует, чтобы `requests/` содержал историю всех инцидентов — это база для поведенческого аудита и ретроспектив. Пустая директория `requests/` — M9 первого еженедельного аудита.

## Вердикт

**approved** Координатором (bootstrap: Директор Governance ещё не подгружен в сессию; первый работающий цикл комиссии будет после перезапуска Claude Code).

## Действия

- Правило в `CLAUDE.md` — остаётся
- Заявка закрыта, сохранена как исторический прецедент
- Следующий аналогичный инцидент обрабатывается через полный цикл комиссии

## Связанные находки аудита

- M9 в `docs/governance/audits/weekly/2026-04-15-first-audit.md`
